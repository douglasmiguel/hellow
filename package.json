{
  "name": "hellow",
  "private": true,
  "version": "0.2.0-beta.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "latest",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest"
  }
}
